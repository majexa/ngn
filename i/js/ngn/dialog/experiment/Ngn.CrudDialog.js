Ngn.CrudDialog = new Class({
  Extends: Ngn.Dialog.RequestForm.Json,
  options: {
    // @requiresBefore [path/to/modelName.js]
    // formTmpl: Ngn.formTmpl.modelName,
    // url: serverUrl + '/api/v1/modelName'
  }
});
